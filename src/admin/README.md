# Admin

Future home for admin routes, request logs, static English page handling, and Cloudflare usage helpers.

Extraction rule: KV logging remains opt-in and visible admin UI text remains English.

