# Proxy

Future home for proxy path/query parsing, preferred endpoint expansion, proxy resolution cache, and endpoint health records.

Extraction rule: preserve target-aware cache keys and optional KV-cache semantics.

