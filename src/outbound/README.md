# Outbound

Future home for TCP forwarding, staggered dialing, connector adapters, SOCKS5, HTTP CONNECT, HTTPS CONNECT, TURN, SSTP, and TLS helper code.

Extraction rule: preserve timeout, cleanup, and fallback behavior exactly unless a benchmark-backed tuning change is planned separately.

